#include <stdio.h>
#include <math.h>
#include <stdlib.h>
#include <stdbool.h>
#include <string.h>

int main(void){
    bool FLAG, RESULT;
    int A, B;
    A = 5;
    B = 10;
    FLAG = .TRUE.;
    if(((A < B) && FLAG)){
    RESULT = .FALSE..FALSE.;
    printf("%d\n", 'A is less than B');
    } else {
    RESULT = .FALSE.;
    printf("%d\n", 'A is >= B');
    }
    return 0;
}
